@page "/AddPet/{StreetName}/{HouseNumber:int}/{IdOfChild:int}"
@using Family_Assignment.Data
@inject IFamilyReader fileReader
@inject NavigationManager NavMgr
@* <h3>AddPet</h3> *@

@if (IdOfChild == 0)
{
    <EditForm Model="@petToAdd" OnValidSubmit="@AddNewPetToChild">
        <div class="bg-green-100 py-32 min-h-screen px-0">
      <!--   tip; mx-auto -- jagab ilusti keskele  -->
      <div class="bg-white p-10 md:w-3/4 lg:w-1/2 mx-auto">
    
        
    
          <!--       flex - asjad korvuti, nb! flex-1 - element kogu ylejaanud laius -->
          <div class="flex items-center mb-5">
              <!--         tip - here neede inline-block , but why? -->
              <label for="name" class="inline-block w-20 mr-6 text-right 
                                     font-bold text-gray-600">First name</label>
              <br /> <InputText class="flex-1 py-2 border-b-2 border-gray-400 focus:border-green-400 
                                                                           text-gray-600 placeholder-gray-400
                                                                           outline-none" @bind-Value="petToAdd.Name"/>
          </div>
          <div class="flex items-center mb-5">
              <!--         tip - here neede inline-block , but why? -->
              <label for="name" class="inline-block w-20 mr-6 text-right 
                                           font-bold text-gray-600">Last name</label>
              <br /> <InputNumber class="flex-1 py-2 border-b-2 border-gray-400 focus:border-green-400 
                                                                                 text-gray-600 placeholder-gray-400
                                                                                 outline-none" @bind-Value="petToAdd.Age"/>
           </div>
           <div class="flex items-center mb-5">
               <!--         tip - here neede inline-block , but why? -->
               <label for="name" class="inline-block w-20 mr-6 text-right
                                            font-bold text-gray-600">Hair color</label>
               <br /> <InputText class="flex-1 py-2 border-b-2 border-gray-400 focus:border-green-400
                                                                                  text-gray-600 placeholder-gray-400
                                                                                  outline-none" @bind-Value="petToAdd.Species"/>
           </div>
           
    
          <div class="text-right text-red-700">
              <DataAnnotationsValidator/> <ValidationSummary/>
              <button class="py-3 px-8 bg-green-400 text-white font-bold" @onclick="@AddNewPetToFamily">Submit</button>
          </div>
    
        
      </div>
    </div>
    </EditForm>
}
else
{
    <EditForm Model="@petToAdd" OnValidSubmit="@AddNewPetToFamily">
        <div class="bg-green-100 py-32 min-h-screen px-0">
      <!--   tip; mx-auto -- jagab ilusti keskele  -->
      <div class="bg-white p-10 md:w-3/4 lg:w-1/2 mx-auto">
    
        
    
          <!--       flex - asjad korvuti, nb! flex-1 - element kogu ylejaanud laius -->
          <div class="flex items-center mb-5">
              <!--         tip - here neede inline-block , but why? -->
              <label for="name" class="inline-block w-20 mr-6 text-right 
                                     font-bold text-gray-600">First name</label>
              <br /> <InputText class="flex-1 py-2 border-b-2 border-gray-400 focus:border-green-400 
                                                                           text-gray-600 placeholder-gray-400
                                                                           outline-none" @bind-Value="petToAdd.Name"/>
          </div>
          <div class="flex items-center mb-5">
              <!--         tip - here neede inline-block , but why? -->
              <label for="name" class="inline-block w-20 mr-6 text-right 
                                           font-bold text-gray-600">Last name</label>
              <br /> <InputNumber class="flex-1 py-2 border-b-2 border-gray-400 focus:border-green-400 
                                                                                 text-gray-600 placeholder-gray-400
                                                                                 outline-none" @bind-Value="petToAdd.Age"/>
           </div>
           <div class="flex items-center mb-5">
               <!--         tip - here neede inline-block , but why? -->
               <label for="name" class="inline-block w-20 mr-6 text-right
                                            font-bold text-gray-600">Hair color</label>
               <br /> <InputText class="flex-1 py-2 border-b-2 border-gray-400 focus:border-green-400
                                                                                  text-gray-600 placeholder-gray-400
                                                                                  outline-none" @bind-Value="petToAdd.Species"/>
           </div>
           
    
          <div class="text-right text-red-700">
              <DataAnnotationsValidator/> <ValidationSummary/>
              <button class="py-3 px-8 bg-green-400 text-white font-bold" @onclick="@AddNewPetToChild">Submit</button>
          </div>
    
        
      </div>
    </div>
    </EditForm>
}